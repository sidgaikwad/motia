import { createMockLogger, CapturedEvent, setupLoggerMock } from '@motiadev/test'

test('logger logs info message', () => {
  const logger = createMockLogger()

  logger.info('Test message')

  // Assert that info() was called with 'Test message'
  expect(logger.info).toHaveBeenCalledWith('Test message')

  // OR check how many times it was called
  expect(logger.info).toHaveBeenCalledTimes(1)

  // OR if you want to inspect all logged messages:
  console.log('Info calls:', logger.info.mock.calls)
})
